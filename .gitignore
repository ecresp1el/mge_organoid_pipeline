.Rproj.user
.Rhistory
.RData
.Ruserdata

# Guardrail: Slurm does NOT expand shell variables in `#SBATCH --output/--error`.
# Submitting templates that used `${PROJECT_ROOT}` previously created a literal
# `${PROJECT_ROOT}/` directory inside the repo. Keep runtime artifacts out of git.
${PROJECT_ROOT}/

# Python notebook runtime artifacts
__pycache__/
.ipynb_checkpoints/
python_notebooks/**/*.h5ad
